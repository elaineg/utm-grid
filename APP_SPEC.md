# UTM Grid
Purpose: a no-account bulk UTM campaign URL builder for marketers — edit 20–50 links in a spreadsheet-style grid with naming-convention linting (both governed allowed-VALUES and a structured campaign-name composition template) and CSV round-trip; client-side by default, with an optional server-persisted shared Team Workspace addressed by a secret link, plus a shareable read-only style-guide page per workspace.
Problem: A demand-gen / lifecycle marketer launching a campaign each week must tag 20–50 links with consistent UTM parameters across channels (email, paid social, organic social, partners), and a single casing/spelling inconsistency silently splits campaign data in GA4 — discovered only weeks later during reporting.
Beats alternative: Teamcamp UTM Bulk Link Creator (free, no account) — the best free bulk tool — requires hand-typed pipe-delimited per-row overrides, has no CSV import (links already live in a campaign planning sheet), no required-param or cross-row consistency linting (only a lowercase checkbox), and no presets. UTM Grid replaces pipe syntax with an editable grid, adds CSV import AND export for round-tripping with the existing campaign sheet, lints conventions as you type — including flagging near-duplicate values across rows (e.g. `spring_sale` vs `Spring-Sale`), the GA4 data-splitting failure no existing free tool catches — and saves channel presets in localStorage. Cuts a weekly 30-link batch from ~15 min to ~5 min.

Core flows:
1. **Edit links in a grid with lint-as-you-type.** The page opens on an editable spreadsheet-style grid: one row per link, columns for base URL, utm_source, utm_medium, utm_campaign, utm_term, utm_content, and a read-only generated-URL column that updates live. Rows can be added, duplicated, and deleted. Linting runs on every edit and marks offending cells with a visible warning (icon/color + hover or inline message): (a) required params missing — utm_source, utm_medium, utm_campaign required by default, toggleable; (b) case/space rules — lowercase-only and no-spaces on by default, each toggleable; (c) cross-row consistency — values in the same UTM column that match after normalizing case and separators (`-`, `_`, space) but differ literally are flagged on every affected cell, naming the conflicting variants; (d) **off-spec values (governed UTM taxonomy)** — a **UTM Spec panel** lets the user define, per UTM field (utm_source / utm_medium / utm_campaign / utm_term / utm_content), a list of canonical **allowed values** (the org's taxonomy). When a field has a non-empty allowed-value list: the cell input offers those values as autocomplete suggestions (HTML datalist), and any non-empty cell value not in the list (compared case-insensitively) is flagged with an **off-spec** lint warning that names the **nearest allowed value** (closest by normalized match, else shortest Levenshtein edit distance) and offers a one-click **"Fix to <nearest>"** action that sets the cell to that allowed value and re-runs lint. Off-spec linting is additive to the case/space/cross-row rules and toggleable like them (a single "Enforce UTM Spec" toggle; when off, no off-spec warnings and no datalist suggestions). A copy button per row copies the generated URL; a copy-all button copies all generated URLs. **Responsive layout (mobile card view, ≤640px):** the grid is purely CSS-responsive (Tailwind breakpoint classes — no JavaScript viewport detection, so no SSR/hydration mismatch and no double-mounted dual-layout component). On wide viewports (`sm` and up) the spreadsheet-style table renders unchanged. On narrow viewports (≤640px) each row renders instead as a vertical **card**: every UTM field is a stacked label-over-input pair (so no horizontal scrolling is needed to reach a field), the generated-URL and per-row copy/duplicate/delete/select controls are full-width and tap-friendly (≥44px touch targets), and lint warnings render inline under their field. All controls remain reachable and hittable on a phone — no sticky/overlay element occludes a checkbox or button. The bulk toolbar, Campaigns/UTM Spec/preset panels, Import/Export, and "Copy share link" all stack and stay reachable on mobile. A shared grid opened from a copied share link renders in this card view on a phone and is fully legible and usable. **Bulk row/column operations (power-edit, fully client-side):** each row has a selection checkbox plus a header "select all" checkbox, so bulk actions target either all rows or just the currently-selected subset (when no rows are selected, bulk ops apply to all rows). A bulk toolbar exposes two operations, each on a chosen UTM column (utm_source / utm_medium / utm_campaign / utm_term / utm_content): (a) **Set column** — set that field to one entered value across the targeted rows in a single action (an empty value clears the field across them), so a weekly 20–50 link batch that shares utm_campaign/source/medium is tagged in one move instead of per-row; (b) **Find & replace in column** — replace a value or substring in that column across the targeted rows (e.g. `Spring-Sale`→`spring_sale` everywhere). Both operations run instantly, persist to localStorage like the rest of the grid, trigger no network request, and re-run linting immediately so cross-row consistency and case/space warnings update on the affected cells. The bulk controls use verbs distinct from the per-row controls (Set / Replace vs the row Dup / Delete) so they never read as duplicate controls.
2. **CSV round-trip with an existing campaign sheet, plus Paste & Audit of existing tagged URLs (inbound counterpart) and a whole-grid Launch Check compliance report.** An Import CSV button accepts a file upload, parses it in the browser, shows a column-mapping step (map CSV headers to base URL and each utm_* field; headers matching field names are pre-mapped), and populates the grid — lint warnings apply immediately to imported rows. An Export CSV button downloads a file with one row per grid row containing the base URL, every utm_* column, and the full generated URL. Exporting and re-importing the same file reproduces the grid exactly. **Paste & Audit existing UTM URLs (the audit-INBOUND counterpart to the build-OUTBOUND grid/CSV flows):** a clearly-labeled **"Audit URLs"** entry point on the main builder — verb DISTINCT from "Import CSV" so the two never read as the same control — opens a textarea where the user pastes full, already-tagged URLs, one per line (inherited from a sheet or pre-launch QA). On submit each non-empty line is parsed **entirely in the browser** (no network): split into the base URL (scheme+host+path with any non-utm query params preserved, e.g. a base `?ref=1` is kept) minus the `utm_*` params, and the utm_source / utm_medium / utm_campaign / utm_term / utm_content values are extracted from the query string into their columns. Malformed lines (not parseable as URLs) are counted and named in a **parse summary** (e.g. "1 line skipped") — never silently dropped, never a crash; valid lines still import. Parsed rows populate the grid using the **SAME unsaved-edits guard** the share-link / Open-campaign load uses (confirm before replacing a dirty working grid; append-vs-replace is acceptable), and linting runs immediately on the imported rows — missing-required, case/space, cross-row consistency, AND off-spec vs the current UTM Spec — exactly as for typed input. Works in the mobile card view (≤640px).
   - **Launch Check (whole-grid compliance report — pre-launch QA over what's already in the grid).** A prominent, clearly-labeled **"Run Launch Check"** action — verb DISTINCT from "Audit URLs", "Import CSV", and from all share actions ("Copy share link" / "Create shared workspace" / "Copy workspace link" / "Share style guide"), so it never reads as the same control — available on BOTH the main builder and on `/w/<id>` workspace pages. The existing inline lint-as-you-type marks one cell at a time; Launch Check answers the different question "is the WHOLE batch ready to ship?" by running the **full existing lint suite across ALL current grid rows at once** (reuses `lintRows` — NO new lint logic) and rendering a **Compliance Report** as a full-width SUMMARY panel ABOVE the grid in normal page flow (NOT a side panel, NOT inside grid columns). The report shows: **total links checked**, **# passing** (rows with zero warnings), **# with issues**; then issues grouped by issue type/field, each line naming the **row number + which cell/field + the existing lint message** (required, lowercase, no-spaces, inconsistent, off-spec, invalid-url, base-utm, off-template). A fully-clean grid shows a clear **"All N links pass"** success state. **Export:** a **"Download report (CSV)"** button downloads a CSV with one row PER violation — columns: row #, base URL, field, value, issue type, message (a single all-clear row when the grid is clean); a **"Copy summary"** button copies a plain-text summary to the clipboard with a peripherally-unmissable **"Copied!"** confirmation (green-fill-in-place + aria-live). **Strictly READ-ONLY of current state:** Launch Check NEVER mutates the grid, NEVER autosaves, and makes NO network request on the builder and NO POST/PUT on `/w/<id>` (it reads the already-loaded workspace state with no extra write — critical so the report can't clobber a workspace). A cold open triggers no Launch Check and no network. **Mode-aware copy:** any "client-side / no network" wording is accurate on the server-backed `/w/<id>` page. Reuses existing state + lint engine — NO new server schema/table, no new credential. Reachable and operable in the mobile card view at 375px (scorecard panel + both export/copy buttons stack, no horizontal scroll, no sticky/overlay occludes the controls); desktop ≥640px unchanged.
3. **Named saved state in localStorage — channel presets AND a Campaigns library.** Two complementary kinds of reusable saved state, both 100% client-side (localStorage only — no accounts, no server, no network):
   - **Channel presets:** save the current values of any subset of UTM fields (e.g. utm_source=facebook, utm_medium=paid_social) as a named preset ("Paid Social"), then apply a preset to a selected row or to new rows so weekly batches start pre-filled.
   - **UTM Spec (governed taxonomy):** the per-field allowed-value lists and the "Enforce UTM Spec" toggle persist in localStorage alongside presets/campaigns, are saved INSIDE each named campaign's state, and ride inside the share-link fragment (Flow 4) — so opening a saved campaign or a shared grid adopts that grid's UTM Spec, propagating the team's taxonomy. This is the convention-propagation loop: one marketer defines the org taxonomy and shares it; the teammate's grid then enforces it.
   - **Campaign Naming Template (governed STRUCTURE of utm_campaign — distinct from UTM Spec, which governs allowed VALUES per field):** a **Naming Template panel** — a first-class, visually DISCOVERABLE sibling to the UTM Spec panel (not buried) — lets the user define an ORDERED list of named **segments** for `utm_campaign` (e.g. segment 1 `quarter`, segment 2 `channel`, segment 3 `audience`) plus a chosen **separator** (default `_`, also `-`). Each segment optionally carries an **allowed-token list** (reusing the UTM Spec allowed-value-list UX pattern). Segments can be added and removed (reorder is a nice-to-have). A per-row **"Build name" composer** offers one control per segment — a dropdown when that segment has an allowed-token list, free text otherwise — and assembles a compliant `utm_campaign` joined by the separator (e.g. quarter=`2026q3`, channel=`paidsocial`, audience=`retargeting` → `2026q3_paidsocial_retargeting`), writing it into that row's utm_campaign cell and re-linting. A single **"Enforce naming template"** toggle (independent of "Enforce UTM Spec"): when ON, any NON-EMPTY utm_campaign value not matching the pattern gets an **off-template** lint warning that names exactly what's wrong — either (a) wrong segment count after splitting on the separator (e.g. "expected 3 segments, found 2"), or (b) a segment whose token is not in that segment's allowed-token list (naming the offending segment + its allowed tokens). This is ADDITIVE to the case/space/cross-row/off-spec rules and toggleable independently; when OFF there are no off-template warnings and no composer enforcement. The template (segment defs + separator + enforce toggle) persists in localStorage alongside presets/UTM-Spec, is saved INSIDE each named campaign's state, and rides inside the share-link fragment AND the Team Workspace (/w/<id>) payload (another field in the same serialized grid+spec state — no new schema, no new credential, free-tier) — so opening a saved campaign, a share link, or a workspace adopts that grid's naming template. This extends the convention-propagation loop from allowed-VALUES to campaign-name STRUCTURE. Fully client-side (no network for template/composer/lint), and reachable/operable in the mobile card view at 375px with no sticky/overlay occluding controls.
   - **Campaigns library (personal-accumulation layer):** save the ENTIRE current grid — every row (base URL + all utm_* fields), the active lint-rule toggles, AND the current UTM Spec (allowed-value lists + enforce toggle) — as a NAMED campaign (e.g. "Black Friday"), building up a reusable library the user returns to each week. A Campaigns panel lists saved campaigns by name with their link count and last-saved time. From it the user can: **Open** a campaign (load it as the working grid, with the same unsaved-edits guard the share-link load uses — confirm before replacing a dirty working grid), **Save as campaign** (name + save current grid; saving under an existing name updates it after a confirm-overwrite), **Duplicate** a campaign (copies to "<name> copy"), and **Delete** a campaign (confirm). A first-ever visit shows an empty library with a one-line hint.
   - Presets, the Campaigns library, lint-rule toggles, and the live working-grid rows themselves all persist in localStorage across reloads (a refresh mid-edit never loses the batch). Opening a saved campaign or a share link never silently clobbers unsaved working-grid edits.
   - From the Campaigns panel the user can also **Rename** a campaign: clicking Rename reveals an inline pre-filled name field (Enter commits, Esc cancels); the campaign's id, rows, and settings are unchanged; renaming to the same name is a no-op; renaming to a name that collides with a different campaign prompts a confirm-overwrite dialog before replacing. A search/filter input at the top of the Campaigns list filters visible cards by name substring (case-insensitive).
4. **Share & sync the grid — one-shot link OR live Team Workspace.** Two rungs of the same sharing ladder; pick the level of persistence the team needs.
   - **Rung 1 — one-shot share link (client-side, no account, no server, unchanged).** A "Copy share link" button encodes the entire current grid state — every row's base URL and all utm_* fields, the active lint-rule toggles, AND the current UTM Spec (per-field allowed-value lists + the "Enforce UTM Spec" toggle) — into the URL fragment (hash), compressed, and copies the full shareable URL to the clipboard with a peripherally-unmissable "Copied!" confirmation. Nothing is sent to any server (the state lives only in the hash). Opening a URL that contains a share fragment rehydrates the grid to exactly the shared state — including the shared UTM Spec, which then governs off-spec linting and datalist suggestions for the visitor — and shows a "Loaded shared grid (N links)" confirmation; the visitor's own localStorage is not overwritten until they edit. This turns a single-user batch into a team handoff — a marketer sends a teammate a link instead of a CSV — while preserving the zero-network privacy prop. This rung is a frozen snapshot: opening it does not sync back.
   - **Rung 2 — Team Workspace (server-persisted, secret-link, last-write-wins, no auth).** A separate **"Create shared workspace"** action (distinct from "Copy share link") POSTs the current workspace payload — the grid rows (base URL + all utm_* fields), the active lint-rule toggles, and the current UTM Spec (per-field allowed-value lists + "Enforce UTM Spec" toggle) — to the server (POST `/api/workspace`), which stores it in a Turso DB and returns a freshly generated, unguessable secret workspace id (≥ 22 url-safe chars). The app then navigates to `/w/<id>` and copies the full `https://<host>/w/<id>` link to the clipboard with a peripherally-unmissable "Copied!" confirmation. The personal **Campaigns library is NOT part of the workspace payload** — the workspace is one shared grid + its taxonomy (the team source-of-truth); the Campaigns library stays local. **`/w/<id>` route:** opening this URL fetches the workspace data from the server (GET `/api/workspace/<id>`) **via a fetch effect — never a useState lazy initializer** (avoids the SSR/hydration mismatch lesson) — and renders the full utm-grid editor seeded with that exact data (rows + lint toggles + UTM Spec). The page shows a clear **"Team Workspace — synced" banner** with a "Copy workspace link" button. Edits to the grid/spec on a `/w/<id>` page **autosave** to the server (debounced ~800ms PUT `/api/workspace/<id>`), **last-write-wins** (no merge, no conflict UI, no presence, no realtime). Opening the same `/w/<id>` link on another device/browser shows the same data on load / after refresh. A workspace whose id doesn't exist shows a clear **"Workspace not found"** state with a link back to the main builder — never a crash or blank. **The visitor's own localStorage working grid is NOT touched by being on a `/w/<id>` page** — workspace state is separate from the local default grid. **Privacy posture:** no login, no email, no account; the secret URL is the capability; the server stores ONLY the workspace payload plus a user-typed editor label (no IP, no identity, no cookies — zero-identity).
   - **Workspace History & Attribution (makes last-write-wins SAFE — `/w/<id>` only).** Because workspace autosave is last-write-wins with no merge, this rung keeps a server-side **version history** so a clobbered grid is always recoverable, plus lightweight human attribution. Each autosave (PUT) appends a version SNAPSHOT to a Turso table `workspace_versions(id INTEGER PRIMARY KEY AUTOINCREMENT, workspace_id TEXT, data TEXT, editor TEXT, created_at INTEGER)`; creating a workspace seeds **version 1**. History is **CAPPED at ~25 newest versions per workspace** (oldest pruned) to stay tiny on the free tier; a save identical to the latest version need not create a new version (dedupe). A **"History" panel on `/w/<id>`** lists versions newest-first, each with a relative time ("2 min ago") and the editor label; the user can **Preview** a version (read-only view of that version's grid/spec, no overwrite) and **Restore this version** — restore is **NON-DESTRUCTIVE**: it PUTs the chosen version's data back as the new current state (reusing the autosave path), which itself becomes a new version, so the clobbered state is never lost. **Optional attribution (anonymous-first):** a small **"Editing as: [name]"** control on `/w/<id>` lets the visitor set a display name, stored in **localStorage per browser** (default e.g. "Anonymous"); it is never required, never blocks editing, and creates no account/email. The name rides with each autosave (becomes that version's `editor`), and is shown in the synced banner ("last edited by <name> <relative-time>") and on each History entry. **GET `/api/workspace/<id>/history`** returns the capped version list (metadata + data) for the panel; **restore reuses the existing PUT autosave path** (restore = PUT the chosen version's data). **Hard invariants:** none of this exists off `/w/` pages — the main builder, Rung 1 one-shot share link, Campaigns library, presets, UTM Spec, bulk ops, and CSV are all unchanged and a cold open triggers no history; a brand-new workspace has exactly one initial version and an otherwise-empty history; anonymous-first is preserved (display name optional and local, no auth, no email, no server-side identity beyond the free-text editor label); free-tier (Turso only, history capped per workspace, no new credential).
   - **Team UTM Style Guide (shareable READ-ONLY governance reference — `/w/<id>/guide`).** A separate route that renders the workspace's accumulated standard as a single legible, human-readable, link-shareable reference page for anyone who needs to KNOW how this team tags links (a teammate, an agency, a contractor) WITHOUT editing. It renders **read-only FROM THE EXISTING WORKSPACE PAYLOAD** (reuses GET `/api/workspace/<id>` — no new schema, no new table, no new credential): (a) the **UTM Spec** allowed-values per field (utm_source/medium/campaign/term/content); (b) the **Campaign Naming Template** — ordered segments + separator + per-segment allowed tokens — WITH a worked example `utm_campaign` assembled from the template; (c) the **active lint rules** (required params, lowercase-only, no-spaces, Enforce UTM Spec, Enforce naming template — whichever are ON); (d) the casing/separator conventions in plain language. The page is **TRULY READ-ONLY**: it makes **NO PUT/POST, never autosaves, never writes to the workspace or localStorage** — it only GETs (critical: a read-only page that writes back can clobber workspace state). A clear, **prominent CTA** ("Open the editable workspace" → `/w/<id>`) invites the reader into the editor — the viral on-ramp turning a non-user recipient into a user. **Discoverability:** the editable `/w/<id>` page exposes a first-class, visually distinct **"Share style guide"** action (NOT buried at the bottom) that copies the `/w/<id>/guide` link with a peripherally-unmissable **"Copied!"** confirmation. A workspace id that doesn't exist → the guide shows a clear **"Workspace not found"** state with a link back to the builder (mirrors `/w/<id>` not-found), never a crash/blank. **Mode-aware copy:** any privacy/"client-side" wording on the guide is accurate for a server-backed page (no stale zero-network claims). Free-tier: Turso only (reuses workspace data), no RESEND, no Stripe, no new env var.

Success checks:
- Entering base URL `https://example.com/sale`, utm_source `newsletter`, utm_medium `email`, utm_campaign `spring_sale` in a row shows `https://example.com/sale?utm_source=newsletter&utm_medium=email&utm_campaign=spring_sale` in that row's generated-URL column.
- Clearing utm_medium in a row (with default rules) shows a visible missing-required-param warning on that cell, and the warning disappears when a value is entered.
- Typing `Spring Sale` in utm_campaign (default rules) shows a lint warning on that cell mentioning uppercase and/or spaces.
- Entering `Spring-Sale` in utm_campaign when another row already has `spring_sale` shows a cross-row consistency warning on BOTH cells that names the conflicting variants; making the values identical clears the warning on both.
- In the UTM Spec panel, adding allowed values `twitter` and `facebook` for utm_source (with "Enforce UTM Spec" on), then typing utm_source `twiter` (a typo) in a row shows an off-spec lint warning on that cell that names the nearest allowed value `twitter`; clicking "Fix to twitter" sets the cell value to `twitter` and the off-spec warning disappears.
- With utm_source allowed values `newsletter`,`facebook` and Enforce UTM Spec on, the utm_source cell input exposes those values as datalist autocomplete suggestions; typing an exactly-allowed value (`newsletter`) shows no off-spec warning.
- Toggling "Enforce UTM Spec" off removes all off-spec warnings and the datalist suggestions; toggling it back on restores them. The UTM Spec (allowed-value lists + enforce toggle) persists across a page reload.
- Defining a UTM Spec (utm_source allowed `newsletter`,`facebook`), saving the grid as campaign `Q3 Taxonomy`, reloading, clearing/altering the spec, then Open `Q3 Taxonomy` restores the saved spec (utm_source allowed values back to `newsletter`,`facebook`) so the loaded grid enforces that taxonomy.
- Building a 3-row grid with utm_campaign empty in all rows, then in the bulk toolbar choosing column `utm_campaign`, entering `black_friday`, and clicking **Set column** (with no rows selected → applies to all) sets utm_campaign to `black_friday` in all 3 rows at once and updates each generated-URL cell to include `utm_campaign=black_friday`; the change persists across a page reload.
- Selecting only rows 1 and 3 via their checkboxes, then Set column `utm_source` to `newsletter`, sets utm_source to `newsletter` in rows 1 and 3 only and leaves row 2 unchanged; the header "select all" checkbox selects/clears every row's checkbox.
- With rows containing utm_campaign values `Spring-Sale` and `spring_sale` (a cross-row consistency warning visible on both), choosing column `utm_campaign`, Find `Spring-Sale` Replace `spring_sale`, and clicking **Find & replace in column** makes both values literally `spring_sale` and the cross-row consistency warning disappears from both cells (lint re-runs immediately).
- Doing a Set column with an empty value clears that utm field across the targeted rows (the cells become empty and, if the field is required, a missing-required-param warning appears on them).
- The bulk Set / Find & replace operations trigger no network request (verifiable in the DevTools Network tab after initial page load).
- Clicking Export CSV downloads a CSV containing one row per grid row with columns for the base URL, each utm_* parameter, and the full generated URL.
- Importing a CSV with headers `url,source,medium,campaign` opens a column-mapping step; after mapping, the grid contains one row per CSV data row with those values, and lint warnings appear on any imported values that violate the rules.
- Exporting the grid to CSV and importing that file back reproduces the same rows and values (round-trip).
- Clicking "Audit URLs" and pasting `https://example.com/sale?utm_source=Newsletter&utm_medium=email&utm_campaign=spring_sale` then `https://example.com/buy?utm_source=newsletter&utm_medium=Email&utm_campaign=Spring-Sale` (one per line) produces 2 grid rows with base URLs `https://example.com/sale` and `https://example.com/buy` and the utm_* values decomposed into the correct columns; cross-row consistency warnings flag `Newsletter` vs `newsletter` (utm_source) and `spring_sale` vs `Spring-Sale` (utm_campaign), and a case warning flags `Newsletter` and `Email`.
- Pasting (via Audit URLs) `https://example.com/x?ref=x&utm_source=fb` keeps `ref=x` on the base URL (base column shows `https://example.com/x?ref=x`) and extracts `utm_source=fb`; that row's generated-URL column reproduces the full URL with `ref=x` and `utm_source=fb` each appearing exactly once (no duplicated params).
- Pasting a block that includes the malformed line `not a url` alongside valid URLs shows a parse summary reading "1 line skipped" (and does not crash); the valid lines still import as rows.
- With a UTM Spec defined (utm_source allowed `newsletter`,`facebook`, Enforce UTM Spec on), pasting `https://example.com/p?utm_source=twiter` via Audit URLs produces a row whose utm_source cell shows an off-spec lint warning naming the nearest allowed value `newsletter`/`facebook` — identical to typing `twiter` by hand.
- Clicking "Audit URLs" and submitting pasted URLs triggers no network request (verifiable in the DevTools Network tab after initial page load); the Audit URLs entry point, textarea, and submit are reachable and operable at a 375px-wide viewport without horizontal scroll.
- Building a 3-row grid where row 2 has utm_medium cleared (missing required) and row 3 has utm_campaign `Spring Sale` (uppercase + space), then clicking **"Run Launch Check"** shows a **Compliance Report** as a full-width panel ABOVE the grid reading total **3**, **1** passing, **2** with issues, and listing a missing-required line naming **row 2 + utm_medium** and case/space line(s) naming **row 3 + utm_campaign** with the existing lint message.
- Building a fully-clean 2-row grid (all required fields present, lowercase, no spaces, no cross-row conflicts), then clicking **"Run Launch Check"** shows **"All 2 links pass"** and lists no violations.
- On the 3-row grid above (2 violations), clicking **"Download report (CSV)"** downloads a CSV with **one row per violation** and columns row #, base URL, field, value, issue type, message; on the clean 2-row grid the downloaded CSV contains a single all-clear row.
- Clicking **"Copy summary"** shows a visible **"Copied!"** confirmation (it persists through a re-render) and triggers no network request (verifiable in the DevTools Network tab).
- Clicking **"Run Launch Check"** on the main builder triggers NO network request (DevTools Network tab); on a `/w/<id>` page it triggers NO POST/PUT (read-only — verifiable in the Network tab).
- On a `/w/<id>` page, clicking **"Run Launch Check"** produces a Compliance Report over the loaded workspace rows and does NOT alter the workspace: a `GET /api/workspace/<id>` taken before running the check equals a `GET` taken after (no autosave PUT fires as a result of running the check).
- At a 375px-wide viewport, the **"Run Launch Check"** trigger, the Compliance Report panel, **"Download report (CSV)"**, and **"Copy summary"** are all reachable and operable without horizontal scroll and not occluded by any sticky/overlay element.
- A cold open of `/` and of `/w/<id>` shows NO Compliance Report until **"Run Launch Check"** is clicked.
- Saving a preset named `Paid Social` with utm_source `facebook` and utm_medium `paid_social`, reloading the page, and applying it to an empty row fills utm_source and utm_medium with those values.
- Building a 2-row grid (row 1 base `https://example.com/a` utm_source `newsletter`; row 2 base `https://example.com/b` utm_source `twitter`), clicking "Save as campaign" and naming it `Black Friday`, shows `Black Friday` in the Campaigns panel listing 2 links; reloading the page, then clicking Open on `Black Friday`, restores exactly those 2 rows with those base URLs and utm_source values.
- With campaign `Black Friday` saved, editing the working grid to different values and clicking Open on `Black Friday` prompts to confirm before replacing the unsaved working grid (it does not silently discard the current edits); confirming loads the saved campaign, cancelling leaves the current grid untouched.
- Saving the current grid under the name `Black Friday` a second time (after edits) prompts a confirm-overwrite and, on confirm, updates the existing `Black Friday` campaign in place rather than creating a duplicate; the Campaigns panel still shows a single `Black Friday` entry with the new link count.
- Clicking Duplicate on `Black Friday` creates a `Black Friday copy` entry with the same rows; clicking Delete on it (and confirming) removes it from the panel, and both changes persist across a reload.
- Saving a campaign named `Black Friday` with 2 links, then clicking Rename and entering `BF 2026` (Enter to commit), updates the card name to `BF 2026` in place, still shows 2 links, and persists across a page reload; the id is unchanged so the "In: BF 2026" pill also reflects the new name if that campaign is open.
- A first visit (empty localStorage) shows an empty Campaigns library with a one-line hint and no saved campaigns.
- Saving, opening, duplicating, and deleting a campaign trigger no network request (verifiable in the DevTools Network tab after initial page load).
- Filling rows and reloading the page restores the same rows (grid edits persist in localStorage); a first visit shows a single empty starter row.
- Entering base URL `https://example.com/p?utm_source=old` with utm_source `src` shows a lint warning on the Base URL cell naming `utm_source`, and the generated URL contains `utm_source=src` exactly once — the grid value replaces the pre-existing param instead of appending a duplicate.
- The page has no login or signup, and editing, importing, and exporting trigger no network requests (verifiable in the browser DevTools Network tab after initial page load).
- Building a 3-row grid with distinct values AND a UTM Spec (utm_medium allowed `email`,`paid_social`, Enforce on), clicking "Copy share link", then opening the copied URL in a fresh/incognito tab shows the same 3 rows with identical base URLs, utm_* values, the same lint warnings, AND the shared UTM Spec in effect (a utm_medium value not in `email`,`paid_social` shows an off-spec warning in the fresh tab); a "Loaded shared grid (3 links)" confirmation is visible. Clicking "Copy share link" shows a clearly visible "Copied!" confirmation, and the click triggers no network request.
- Opening a share link does not overwrite the visitor's pre-existing localStorage grid until the visitor edits a cell (so a teammate previewing a shared grid does not lose their own in-progress batch by merely opening the link).

- Building a 2-row grid with a defined UTM Spec (utm_source allowed `newsletter`,`facebook`, Enforce on) and clicking "Create shared workspace" navigates to a `/w/<id>` URL (id ≥ 22 url-safe chars), copies that full `https://<host>/w/<id>` link to the clipboard (a "Copied!" confirmation is visible), and shows a "Team Workspace — synced" banner.
- Opening that same `/w/<id>` URL in a fresh browser with empty localStorage loads exactly those 2 rows and that UTM Spec (a utm_source value not in `newsletter`,`facebook` shows an off-spec warning), and does NOT alter the fresh visitor's local default grid (navigating back to `/` shows the empty starter grid, unchanged).
- On a `/w/<id>` page, editing a cell value and waiting ~1s for the debounced autosave, then reloading the page, shows the edited value persisted (server round-trip).
- Opening `/w/<id>` in a second browser AFTER an edit-and-autosave in the first browser shows the edited value (last-write-wins sync across devices).
- Visiting `/w/` + a nonexistent id (e.g. `/w/does-not-exist-xxxxxxxxxxxxxxxxxxxx`) shows a "Workspace not found" message with a link back to the builder, not a crash or blank page.
- Creating a workspace triggers exactly one network POST (to `/api/workspace`); thereafter, on the main builder, the one-shot "Copy share link" (URL fragment) still works and still triggers no network request — the two rungs coexist.
- At a 375px-wide viewport on a `/w/<id>` page, the "Team Workspace — synced" banner and "Copy workspace link" button are fully visible and do not occlude any grid cell, checkbox, or per-row control — all remain visible and tappable.
- Create a workspace, then open `/w/<id>`: the History panel shows exactly ONE entry (the initial version) labeled with a relative time. Edit one cell value (e.g. set row 1 utm_campaign to `v2_edit`) and wait ~1s for autosave; the History panel now shows TWO entries newest-first, the newest carrying the editor name and a fresh relative time ("just now"/"<1 min ago").
- On `/w/<id>`, set "Editing as:" to `Alex`, then edit a cell and wait for autosave: the synced banner reads "last edited by Alex" with a relative time, and the newest History entry shows editor `Alex`. (Without setting a name, entries show the default "Anonymous".)
- On `/w/<id>`, with a version A where row 1 utm_campaign is `summer` and a current version B where it is `winter`, click Restore on version A: the grid shows `summer` again AND a new newest version is created (so version B's `winter` state is still listed in History and itself restorable) — nothing is lost. Preview on any version shows that version's exact rows read-only without changing the current grid.
- A freshly created workspace (no edits yet) shows a single initial History entry; the editor label defaults to "Anonymous" unless a name was set before creation.
- History is capped: making 30+ distinct autosave edits to one `/w/<id>` workspace leaves the History panel showing at most ~25 entries (the newest), and `GET /api/workspace/<id>/history` returns at most ~25 versions — the `workspace_versions` table does not grow unbounded for that workspace.
- The main builder cold open (`/`) and a Rung 1 one-shot share link create NO version history and make no call to `/api/workspace/<id>/history`; History exists only on `/w/<id>` pages.

- Create a workspace with a UTM Spec (utm_source allowed `newsletter`,`facebook`) and a 3-segment naming template (`quarter`/`channel`/`audience`, separator `_`), then open `/w/<id>/guide`: the page shows those allowed values per field, the template segments + separator WITH a worked example `utm_campaign` (e.g. `2026q3_paidsocial_retargeting`), and the active lint rules — all rendered read-only with NO editable inputs (no text boxes, no toggles, no grid cells to edit).
- Opening `/w/<id>/guide` makes only a GET (to `/api/workspace/<id>`) and NO POST/PUT — verifiable in the DevTools Network tab; reloading the guide and waiting does not trigger any autosave write.
- The `/w/<id>/guide` page has a clear, prominent CTA linking to `/w/<id>` (the editable workspace).
- From `/w/<id>`, clicking the "Share style guide" action copies the `/w/<id>/guide` link to the clipboard and shows a visible "Copied!" confirmation; the copied URL ends in `/guide`.
- Visiting `/w/does-not-exist-xxxxxxxxxxxxxxxxxxxx/guide` shows a "Workspace not found" message with a link back to the builder, not a crash or blank page.
- At a 1280px-wide viewport on `/w/<id>` with Enforce UTM Spec on and a long generated URL (an off-spec value showing an inline warning), the editable grid table does NOT cause horizontal PAGE overflow — no horizontal scrollbar on the page body (the recurring layout residual is resolved).
- At a 375px-wide viewport, the `/w/<id>/guide` page is fully legible with no horizontal scroll — allowed-value lists, template segments, the worked example, and the CTA all wrap and remain readable.

- At a 375px-wide viewport (a phone), the grid renders as stacked vertical cards — one card per row — with NO horizontal page scroll required to read or edit any UTM field; each field shows its label above its input. (At ≥640px the spreadsheet table renders instead.)
- At 375px, every per-row control — the row selection checkbox, copy, duplicate, and delete — is fully tappable (not occluded by any sticky/overlay element) and at least ~44px in its tappable dimension; tapping each performs its action.
- At 375px, the bulk toolbar (column picker + value + Set / Find & replace), the Campaigns panel, the UTM Spec panel, Import/Export CSV, and "Copy share link" are all reachable and operable without horizontal scrolling.
- Opening a copied share link on a 375px viewport rehydrates the grid and renders it in the mobile card view, fully legible (the "Loaded shared grid (N links)" confirmation is visible and each shared row's fields are readable without horizontal scroll).
- The desktop spreadsheet table layout and all its behaviors are unchanged at ≥640px (resizing a desktop browser from wide to narrow swaps table↔cards with no loss of grid state).

- In the Naming Template panel, defining 3 segments `quarter`,`channel`,`audience` with separator `_`, then using a row's "Build name" composer to set quarter=`2026q3`, channel=`paidsocial`, audience=`retargeting` writes `2026q3_paidsocial_retargeting` into that row's utm_campaign cell, and the generated-URL column updates to include `utm_campaign=2026q3_paidsocial_retargeting`.
- With the 3-segment template (`quarter`/`channel`/`audience`, separator `_`) and "Enforce naming template" ON, typing utm_campaign `2026q3_paidsocial` (only 2 segments) in a row shows an off-template lint warning on that cell reading "expected 3 segments, found 2"; completing it to `2026q3_paidsocial_retargeting` clears the warning.
- With the same template where segment `channel` has allowed tokens `paidsocial`,`email` and Enforce naming template ON, typing utm_campaign `2026q3_organic_retargeting` shows an off-template warning naming the offending segment (`channel`) and its allowed tokens (`paidsocial`,`email`); changing `organic` to `paidsocial` clears it.
- Toggling "Enforce naming template" OFF removes ALL off-template warnings (a utm_campaign like `2026q3_paidsocial` no longer warns) and disables composer enforcement; toggling it back ON restores them. Toggling "Enforce naming template" is independent of "Enforce UTM Spec" (turning one off leaves the other's warnings in place).
- Defining the 3-segment template (separator `_`, channel allowed `paidsocial`,`email`) and reloading the page restores the same segment definitions, separator, and enforce toggle (template persists in localStorage).
- Saving the grid as campaign `Q3 Naming` with the template defined, reloading, clearing/altering the template, then Open `Q3 Naming` restores the saved template (3 segments + separator `_` + channel allowed tokens) so the loaded grid enforces that naming structure.
- Building a grid with the template defined and an off-template utm_campaign value `2026q3_paidsocial` (2 segments), clicking "Copy share link", then opening the copied URL in a fresh/incognito tab adopts the same naming template — the off-template utm_campaign shows the "expected 3 segments, found 2" warning in the fresh tab.
- Creating a Team Workspace with the naming template defined, then opening that `/w/<id>` URL in a fresh browser with empty localStorage, loads the same template (an off-template utm_campaign such as `2026q3_paidsocial` shows the off-template warning there) — the template rides in the workspace payload.
- Defining a template, using the Build-name composer, and triggering off-template linting all happen with no network request (verifiable in the DevTools Network tab after initial page load); the Naming Template panel and per-row composer are reachable and operable at a 375px-wide viewport without horizontal scroll.

Out of scope:
- Accounts, auth, payments, or email. The Team Workspace is the only server-persisted state (Turso DB `utm-grid`, tables `workspaces(id TEXT PRIMARY KEY, data TEXT NOT NULL, created_at INTEGER, updated_at INTEGER)` and `workspace_versions(id INTEGER PRIMARY KEY AUTOINCREMENT, workspace_id TEXT, data TEXT, editor TEXT, created_at INTEGER)`, env vars `TURSO_DATABASE_URL` / `TURSO_AUTH_TOKEN`); it is addressed by a secret capability URL with no identity attached. The rest of the app remains fully client-side.
- Realtime collaboration on a workspace: no presence/cursors, no live multi-user merge, no conflict resolution — workspace sync is last-write-wins on debounced autosave, reflected on the other device on load/refresh. No auth or accounts on the workspace this version; access control is the secrecy of the URL only.
- Workspace History is recovery-grade, not a diff tool: no per-field/per-cell diffing UI (a version is restored or previewed whole), no branching/named versions, no comment threads. The editor label is unverified free text (anonymous-first), not an authenticated identity. There is no auth on restore — anyone holding the secret `/w/<id>` link can preview and restore any version; that is acceptable for this version since the URL secrecy is the only access control. History is capped at ~25 versions per workspace, so older history beyond the cap is intentionally not retained.
- The `/w/<id>/guide` style guide is a READ-ONLY rendering of the existing workspace payload — it adds no new schema, table, or credential, and deliberately has NO editing (no inline edit, no PDF/print export, no per-section permalinks); editing is only on `/w/<id>`. This run also resolves the recurring layout residual where, at ~1280px with Enforce on and a long generated-URL column, the editable grid table overflowed and inline warnings forced horizontal scroll — the grid now causes no horizontal page overflow at 1280px.
- Account-based team convention configs with named users/roles (the utm.io/Terminus pay feature). One-shot grid sharing remains client-side via the URL fragment only (no server, no stored registry).
- Link shortening, click tracking, QR codes, or analytics integrations (no GA4 API).
- Direct Google Sheets / Excel integration (CSV files only).
- Single-base-URL parameter-matrix expansion (the Groot Solutions bulk mode).
- Destination-URL reachability checks (basic URL format validation only).

Production URL: https://utm-grid.vercel.app
