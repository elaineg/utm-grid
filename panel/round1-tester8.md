# Rob — Round 1
CLARITY: Yes — Title "UTM Grid" + the one-line subhead ("Bulk-build campaign URLs in an editable grid... CSV import/export") told me what it is in 3 seconds; the spreadsheet grid confirms it.
VALUE: Yes — For a batch of client links the grid + lint + Copy-all beats me hand-typing query strings and fat-fingering an "&"; presets save channel defaults I reuse weekly.
ADVOCACY: 7/10 — Solid and free, but it's a "nice when I have 5+ links" tool, not a daily must; lint that warns-but-doesn't-fix is what holds it back.
LIKES:
- Lint flagged my sloppy "Facebook" and "paid social" inline with clear red hints — catches the exact typos I make.
- Copy all URLs and Export CSV both worked first try; CSV had clean headers + generated_url column, drops straight into a client handoff sheet.
- Presets persist in localStorage (no account, survived my sessions) — save "fb / paid social" once, new rows auto-fill. That's the real time-saver for repeat tagging.
- Dup/Del per row + "https://…" base placeholder make filling a campaign batch fast.
COMPLAINTS (ranked, most important first):
- Lint only WARNS — it left "utm_medium=paid%20social" and uppercase "Facebook" in the actual generated URL. As a one-click "fix to lowercase/underscore" it'd be perfect; right now I still have to manually retype, which is the very work I came to avoid.
- No visible "lowercase/encode automatically" toggle, so a careless paste still ships a broken-looking link to the client.
- For just 1-2 links this is overkill vs. typing the string by hand — value only kicks in at a batch, so I'd reach for it occasionally, not constantly.
- Minor: empty state shows one blank row with no example data, so first-timers don't see what a "good" filled row looks like.
VERDICT_BLOCK: {"id":8,"name":"Rob","clarity":"Yes","value":"Yes","advocacy":7}
