# Dana — Round 1
CLARITY: Yes — h1 "UTM Grid" + subline "Bulk-build campaign URLs in an editable grid... channel presets" told me exactly what it is in under 5s.
VALUE: Yes — replaces my hand-typed Google sheet of UTM links; presets prefill new rows, copy-all dumps clean URLs, and the GA4 consistency linter catches the errors that quietly split my campaign data.
ADVOCACY: 8/10 — I'd screenshot this for the team channel; held back from 9 only by no batch base-URL fill and no built-in channel presets out of the box.
LIKES:
- Whole flow fits in one scroll: grid, presets, lint toggles, copy-all all above the fold.
- Save preset pre-fills from the current row with per-field checkboxes; "New rows use: Paid Social" auto-fills source/medium on every Add row. That's the weekly grind gone.
- Linter is the standout: flagged "Google Ads" (uppercase/space) AND "q3-launch vs q3 launch" inconsistency warning me it'd split GA4 data. No other tool I use does this.
- Copy all URLs worked, showed "Copied", clean newline-separated list ready to paste.
COMPLAINTS (ranked, most important first):
- Ships with ZERO presets — "none yet". I have to build linkedin/google/meta/email from scratch on first use; one click to seed standard channels would make me an instant fan.
- No way to bulk-set the base URL or campaign down a column — I still re-typed "q3-launch" and the acme.com base per row; for 30 links that's still a lot of typing.
- Spaces/uppercase get encoded into the URL (%20) instead of being auto-fixed; the lint warns but won't clean it for me, so I could still ship a broken link if rushing.
- "Copy" per-row vs "Copy all URLs" — fine, but no "Export to Google Sheets" or copy-as-table; Export CSV is close but an extra step.
VERDICT_BLOCK: {"id":5,"name":"Dana","clarity":"Yes","value":"Yes","advocacy":8}
