{"name":"Dana","clarity":"Yes","value":"Yes","advocacy":9}

I am Dana, demand-gen marketer, tag 30+ links every week before Thursday, ruthless about time.

WHAT I DID: Cold-opened. Headline "Clean UTM links for your whole campaign — in one grid" told me the job in one scroll — I stayed. Built a realistic 3-channel campaign (LinkedIn/Paid_Social/"Q3 Launch", newsletter/email, Google/cpc) with deliberately messy casing+spaces. Hit "Auto-fix naming": all 6 dirty cells normalized to lowercase+underscores (q3_launch) across every row in one click — clean generated URLs. Created shared workspace; "Share style guide" copied /w/<id>/guide. Viewed seeded guide + my own + mobile (375px).

THE STYLE GUIDE (new): This is the piece I'd actually use. Clean read-only reference with "Why UTM tags matter" (the Newsletter vs newsletter splitting-data argument is exactly how I justify standards to my agency), allowed values as tidy chips per field, a 2-segment campaign template with a WORKED EXAMPLE (q1_email), and 3 plain-English conventions. My empty workspace showed a graceful "No custom taxonomy defined yet" state instead of a broken page — thoughtful. Fully responsive on phone. Zero console errors anywhere. I would screenshot/paste this link into my team channel today.

FRICTION:
- Seeded guide's campaign "channel" segment (email/social/ppc) doesn't match its own utm_medium allowed values (email/paid_social/cpc). Reads as a self-contradiction at a glance — likely the team's config, but the guide doesn't reconcile the two lists, which would make a teammate ask "so is it cpc or ppc?"
- After Auto-fix the grid collapsed to show only the Generated-URL column (source/medium/campaign columns hidden). URLs were correct but I briefly thought my inputs vanished.
- "Share style guide" only exists after you create a workspace — a solo user tagging a one-off batch never discovers the guide exists.

TO HIT 9-10 (already a 9): reconcile/cross-link the naming-template segment values with the allowed-values lists so the guide can't contradict itself; keep input columns visible after auto-fix (or a clear "X cells fixed, click to expand" affordance). A 10 would let me one-click turn THIS campaign's actual values into the team's allowed-value list, so the guide writes itself from my real grid instead of needing separate setup.
