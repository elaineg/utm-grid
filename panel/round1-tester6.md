# Round 1 — Tester 6 (Aisha, Product designer)

Cold open reads instantly: H1 "Clean UTM links for your whole campaign — in one grid" plus the sub explains job + no-login. As a designer who rarely builds UTMs but got it shared, the craft holds: considered empty states ("No saved campaigns yet — build a grid, then 'Save as campaign'…"), gentle lint affordances ("Contains uppercase letters — use lowercase only ('email'). Fix"). Created a shared workspace in one click → clean /w/ URL, "Team Workspace — synced · saved just now". Set "Editing as: Aisha" — it stuck and showed "last edited by Aisha" + per-entry attribution ("by Aisha" / "by Anonymous"). History copy is the reassurance I'd want: "Every save is kept. Restoring brings a version back without losing the current one." Preview = amber read-only banner with "Restore this version" / "Back to current"; the grid actually reverted in preview, restore non-destructive. Zero console errors throughout.

Craft nits (not blockers): stacked banners (synced + version card + preview) make the header busy at once; lint text wraps mid-phrase in the narrow source/medium columns; the "Editing as" name was free-text Anonymous default — fine, but no avatar/color makes multi-editor attribution feel flat.

CLARITY: Yes
VALUE: Yes
ADVOCACY: 8/10
REASON: History + per-edit attribution + non-destructive restore make a shared grid feel like a trustworthy source-of-truth, and the copy/empty-states are considered enough that I'd advocate; held back from 9 by a busy stacked header and lint text wrapping awkwardly in narrow columns.
